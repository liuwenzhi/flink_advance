目录是process处理函数相关代码演示，包括课程：90到98课内容。
如果一个功能不知道用什么算子，可以无脑直接上process，process函数是最底层的函数，能拿到的信息很多，包括入参，上下文对象（这个非常重要）

flink的四层函数

sql
TableAPI
DataStreamAPI/DatasetAPI
有状态的流处理，即process函数

总结一个时间语义相关的点：
如果处理函数中，用的是事件时间，则需要通过传输的事件信息提取时间，需要等新的数据过来。
如果处理函数中，用的是响应时间（处理时间），则直接取的系统时间。不等新的数据过来，直接会触发timer相关的功能。

